// The reverse() method reverses the order of the elements in the array.

// Example:
const arr=[1,2,3,4,5];
arr.reverse();
console.log(arr); //[ 5, 4, 3, 2, 1 ]

const arr2= ['cow','cat', 'dog','hen'];
arr2.reverse();
console.log(arr2); //[ 'hen', 'dog', 'cat', 'cow' ]