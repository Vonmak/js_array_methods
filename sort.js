// The sort() method sorts the elements of an array in place and returns the sorted array.
// It can take an optional compare function as an argument.

// Example:
const arr= ['cow','hen','cat', 'dog'];
arr.sort();
console.log(arr); //[ 'cat', 'cow', 'dog', 'hen' ]